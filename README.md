## Offline-first vehicle tracking + daily accounting app with dashboard, reports, and PDF/Excel export

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
